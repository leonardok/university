/*
 *  task_queues.cpp
 *  schedsim_rr
 *
 *  Created by Leonardo Korndorfer on 5/23/10.
 *  Copyright 2010. All rights reserved.
 *
 */

#include "task_queues.h"

/*
 * task_queues_class::add_task
 *
 * overview:
 *   add a task pointer by "task" to the queue
 *
 * input:
 *   1. pointer to the task
 *
 * output:
 *   true if added task to the queue
 *   false if could not add the task to the queue
 */
int task_queues_class::add_task(task_class *task){
	return true;
}

/*
 * task_queues_class::rem_task
 *
 * overview:
 *   remove a task pointer by "task" from the queue
 *
 * input:
 *   1. pointer to the task
 *
 * output:
 *   true if removed the task to the queue
 *   false if could not remove the task from the queue
 */
int task_queues_class::rem_task(task_class *task){
	return true;
}