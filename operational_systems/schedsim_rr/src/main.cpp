#include <iostream>

int main (int argc, char * const argv[]) {
    /* 
	 * verify if the files passed are readble and start
	 * a task for each file registering the file content
	 * on the struct task_t
	 */
	
	/*  */
    
    return 0;
}
